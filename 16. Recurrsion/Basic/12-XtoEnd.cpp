#include<bits/stdc++.h>
using namespace std;

string XtoEnd(string str){

	// Base Case
	if(str.length() == 0) return str;

	string ans;
	if(str[0] == 'x') {
		ans = str.substr(1) + 'x';
		XtoEnd(ans);
	}
	else{
		ans = 
	}


}

int main() {
	
	
	
	return 0;
}